import styled from 'styled-components';
import { colorRed } from '../../shared/styles/colors';

export const LogoStyled = styled.div`
  color: ${colorRed};
  font-size: 20px;
  
  a {
    color: inherit;
    text-decoration: none;
  }
`;
